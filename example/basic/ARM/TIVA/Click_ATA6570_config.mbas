module Click_ATA6570_config

include Click_ATA6570_types

const
    _ATA6570_SPI_CFG as uint32_t[3] = (1000000, _SPI_MASTER, ((_SPI_8_BIT or _SPI_CLK_IDLE_LOW) or _SPI_SECOND_CLK_EDGE_TRANSITION))
    _ATA6570_UART_CFG as uint32_t[6] = (9600, 0, _UART_8_BIT_DATA, _UART_NOPARITY, _UART_ONE_STOPBIT, _UART_HIGH_SPEED)
    
end.