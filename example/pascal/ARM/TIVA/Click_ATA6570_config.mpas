unit Click_ATA6570_config;

uses Click_ATA6570_types;

const
    _ATA6570_SPI_CFG : array[3] of uint32_t = (1000000, _SPI_MASTER, ((_SPI_8_BIT or _SPI_CLK_IDLE_LOW) or _SPI_SECOND_CLK_EDGE_TRANSITION)); 
    _ATA6570_UART_CFG : array[6] of uint32_t = (9600, 0, _UART_8_BIT_DATA, _UART_NOPARITY, _UART_ONE_STOPBIT, _UART_HIGH_SPEED); 
    
end.